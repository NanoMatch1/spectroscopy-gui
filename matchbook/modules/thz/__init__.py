"""THz TDS module — self-contained analysis for THz time-domain spectroscopy.

This package can run standalone (no GUI, no framework dependencies) using
the models, loaders, and analysis sub-modules directly.  The ``adapter``
sub-module bridges to the matchbook framework when the GUI is used.
"""
